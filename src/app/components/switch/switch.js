import React from 'react';
import style from './switch.module.css'

const Switch = ({item1, item2, onchange}) => {

    onchange = () => {

    }

    return (
        <div className={style.switch__conainer}>
            <p>hey</p>
        </div>
    );
};

export default Switch